$(document).ready(() => {
  $('#form_forgot_pass').submit((event) => {
    event.preventDefault();
    const email = $.trim($('#email_forgot').val());
    // verificar que existe el email con un json
  });
  // brackets del document.ready
});
